import React from 'react';
import ReactDOM from 'react-dom/client';
import MapScreen from 'modules/map-screen';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MapScreen />
  </React.StrictMode>
);
